export class Curso {
    
    //Vamos lá, antes não era necesário desta forma, mas tudo bem. 
    //a exclamação (!) indica que a variável não será nula, mas temos que garantir isso antes dela ser utilizada. 
    //ou poderia ser a interrogação (?) indicando que a variável é opcional

    idcurso !: number;
    nomecurso !: string;

}